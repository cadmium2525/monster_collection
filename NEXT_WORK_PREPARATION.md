# 次回作業準備: 軽量化・リーダーFoil・レアリティ・奪取UI

調査日: 2026-08-30  
基準コミット: `7f5aa0a` (`v1.18.1`)

この文書は調査結果と実装順を固定するための準備資料です。記載した修正はまだゲームコードへ適用していません。

## 1. 全体軽量化

### 現状

| 対象 | ファイル数 | 容量 |
| --- | ---: | ---: |
| `assets/` | 177 | 44.46 MiB |
| `src/` | 68 | 0.55 MiB |
| GitHub Pages成果物 `dist/` | 250 | 45.15 MiB |
| WebP画像 | 163 | 43.83 MiB |

配信容量の約97%がWebP画像であり、軽量化の主対象は画像。JS/CSSの圧縮だけでは効果が小さい。

画像の内訳:

| フォルダー | 枚数 | 容量 | WebP画像全体に占める割合 | 主な解像度 |
| --- | ---: | ---: | ---: | --- |
| `showcase-fusions/` | 48 | 20.15 MiB | 46.0% | 1024×1365 |
| `showcase/` | 24 | 9.69 MiB | 22.1% | 1024×1365 |
| `breeders/` | 32 | 4.88 MiB | 11.1% | 768×768 / 768×1024 |
| `special-fusions/` | 48 | 4.13 MiB | 9.4% | 主に768×1024、一部切り出し画像 |
| `booster/` | 6 | 1.99 MiB | 4.5% | 1024×1365 |

`showcase-fusions/` と `showcase/` だけで約29.84 MiB、全WebPの約68.1%。ここを最初に処理する。

### 実装方針

1. 元画像は削除せず、変換前後の寸法・容量を一覧化する監査スクリプトを追加する。
2. 1024×1365のカード絵は、カード詳細の最大表示幅とRetina表示を考慮し、まず640×853を候補として品質82～86で比較する。
3. 目・輪郭・細線・暗部の階調が崩れていないか、通常カード、Foil、特殊合体詳細の3画面で目視確認してから一括変換する。
4. `showcase-fusions` → `showcase` → `booster` → `breeders` の順に変換し、各段階でスクリーンショット比較と全テストを行う。
5. 一覧画面は現在、各カードのCSS背景画像URLを一度にDOMへ設定するため、画面を開くと画面外の画像まで取得され得る。単体画像は `<img loading="lazy" decoding="async">` または `IntersectionObserver` で遅延設定し、アトラス画像だけ現行の背景方式を残す。
6. `scripts/build-pages.mjs` は `assets` 全体を複製している。実行時に参照されない原本アトラス・QA用資料を配信対象から除外し、リポジトリには根拠資料として残す。
7. Service Workerは大型画像を初回プリキャッシュから既に除外している。この方針は維持し、画像は初回参照時のみキャッシュする。

### 目標

- `dist/` を45.15 MiBから25 MiB以下へ削減することを第一目標とする。
- ホーム初回表示では大型特別絵を取得しない。
- カード図鑑・管理者一覧を開いても画面外画像を一括取得しない。
- 変換後もカード詳細とFoil表示で肉眼上の品質差をほぼ感じない。

## 2. デッキリーダーのFoil反映

### 原因

- `DeckCollection` が保存している代表情報は `representativeMonsterId`（モンスターマスターID）のみ。
- `deck-screens.js` の保存デッキ一覧、リーダー選択、`tournament-setup-screen.js` は `renderCard` に `definition` しか渡していない。
- Foil・特別絵情報は個別カード資産の `finish` / `artVariantId` にあるため、`cardAsset` を渡さない表示では通常絵・通常加工になる。
- 同じモンスターの通常版とFoil版が同居しても、現在のリーダー選択画面はマスターIDで重複除去するため、どの外観を選んだか区別できない。
- チャンピオン表示も代表マスターIDに一致する最初のカードを選ぶため、同名の通常版が先ならFoilが反映されない。

### 実装方針

今回は保存形式を急に壊さないため、二段階で直す。

1. 共通ヘルパー `representativeCardAsset(cards, representativeMonsterId)` を追加する。
2. 同じマスターIDの候補から、`特別絵+Foil` → `特別絵` → `Foil` → `通常` の順で代表外観を選ぶ。
3. 保存デッキ一覧、リーダー選択、デッキ詳細、大会エントリー、チャンピオン表示の全経路で同じヘルパーを使い、必ず `cardAsset` を描画関数へ渡す。
4. 将来、同キャラクターの通常版をあえて代表にする需要が出た場合のみ `representativeCardInstanceId` を追加する。現時点では「同キャラなら最も特別な所有外観を代表表示」がUIと既存データの互換性を両立する。

### テスト条件

- 同一モンスターの通常版とFoil版がデッキにある場合、保存デッキ一覧・大会エントリー・リーダー選択でFoilになる。
- 特別絵Foilがある場合は特別絵Foilを優先する。
- Foilカードをデッキから外した場合は通常版へ安全にフォールバックする。
- 戴冠スナップショットにFoil代表がある場合、ホームの王者画像でもFoilを再現する。

## 3. 同じカードのRare/Common不統一

### 原因

- `pack-generator.js` の `rarity` はカード固有情報ではなく「どの抽選枠から出たか」で付与されている。
- 同じマスターカードが通常枠から出ると `common`、5枚目の保証枠から出ると `rare` になる。
- `assetStackKey` はrarityをキーに含めないため、未所属資産では同じ外観が一つにまとまる一方、先に保存されたrarityが表示上残り得る。
- マスターデータ側にカード固有rarityの正本がない。

### 実装方針

1. `baseCardRarity(definition)` を一か所に定義し、通常絵のレアリティをカードIDごとに固定する。
2. 特別絵は常に `showcase`、Foilはレアリティではなく加工として独立させる。
3. パックの各抽選枠は「排出カードを選ぶ」だけにし、選ばれた枠からrarityを上書きしない。
4. 提供割合画面と開封演出も同じ `baseCardRarity` を参照する。
5. 既存保存データは読み込み時に正規化し、同じ `masterId + artVariantId + finish + origin` の資産を統合する。枚数は失わない。
6. Rare保証枠は正規化後もRare以上を必ず含む候補プールだけから抽選する。

カードごとのCommon/Rare割り当てはゲームバランスではなく表示・排出設計にも関わるため、実装時に一覧を自動生成して確認する。少なくとも「同じ通常絵カードがCommonとRareの両方になる」状態は禁止する。

### テスト条件

- 同じカードを異なる抽選枠・異なるseedで1000回生成しても通常絵rarityは一種類だけ。
- 5枚入りのRare以上保証は維持される。
- Showcaseはすべてモンスターかつ奪取不可、Foilは加工として別判定のまま。
- 旧データのRare/Common混在を正規化しても数量が減らない。

## 4. 奪取画面の両端カード欠け

### 原因

`styles.css` の `.reward-card-row` が横スクロール可能なFlexboxでありながら `justify-content: center` を使用している。5枚が領域幅を超える端末では、中央揃えによって先頭側がスクロール領域の負方向へ配置され、両端カードの外にはみ出すステータスアイコンや選択時の影も含めて欠ける。

### 実装方針

1. `.reward-card-row` を `justify-content:flex-start` にする。
2. 前後の疑似要素を伸縮スペーサーにし、「収まる時は中央、収まらない時は左右パディングを保った先頭揃え」を実現する。
3. 四隅アイコンと選択影の外側余白として、左右・上下とも最低14pxを確保する。
4. 選択による再描画後も、選んだカードが表示範囲に残るよう必要ならスクロール位置または選択カードIDを復元する。

### テスト条件

- 844×390、iPhone相当safe-areaありで、1枚目・5枚目をそれぞれ選択してもカード本体と四隅アイコンが欠けない。
- 5枚すべてへ横スクロールで到達できる。
- 横幅に余裕がある画面では5枚が中央に見える。
- 詳細ボタン、選択解除、2枚選択の操作を妨げない。

## 5. デッキ編集時の入替候補ソート

### 現状

- 「使用中の40枚」には種類順・TP順・名前順・登録順のソートがある。
- 右側の「入替候補」は固定された表示順で、未所属資産や予備カードが増えるほど目的のカードを探しにくい。

### 実装方針

1. 入替候補側にも独立したソート選択欄を設置する。
2. 使用中40枚のソート状態とは別に保持し、片方を変更してももう片方の並び順を変えない。
3. 最低限、種類順・TPが低い順・TPが高い順・名前順・登録順を使用可能にする。
4. Foil・特別絵・通常絵が混在しても同じカード資産を失わず、表示順だけを変更する。
5. ソート後も選択中の使用カードを `instanceId` で維持し、誤ったカードとの交換を防ぐ。

### テスト条件

- 入替候補を各モードで並べ替えられる。
- 使用中40枚と入替候補が別々のソート状態を維持する。
- ソート前に選択した使用カードがソート後も選択状態を保つ。
- 同名の通常絵・Foil・特別絵を取り違えずに交換できる。

## 6. 長押し時のスマートフォン範囲選択防止

### 現状

- デッキ編集ではカード長押しで詳細を開ける。
- スマートフォンブラウザ本来の長押し動作も同時に発生し、文字の範囲選択、選択ハンドル、コンテキストメニューが表示される場合がある。
- `.deck-builder-screen .game-card` には `user-select:none` があるが、子要素・iOS Safari固有動作・長押し判定中のイベント抑止が十分ではない可能性がある。

### 実装方針

1. デッキ編集画面内のカードと全子要素へ `user-select:none`、`-webkit-user-select:none`、`-webkit-touch-callout:none` を適用する。
2. 長押し対象カードの `contextmenu` を抑止する。
3. 長押し判定中に必要な `pointerdown` / `pointermove` / `pointerup` のみを扱い、通常の短いタップによるカード選択は維持する。
4. 画面全体の範囲選択を禁止せず、カード以外の入力欄や必要な文章操作へ影響させない。
5. スクロール開始を長押しと誤判定しないよう、既存の移動距離によるキャンセル判定を維持・確認する。

### テスト条件

- iPhone Safari相当でカードを長押ししても範囲選択、選択ハンドル、コピー表示が出ない。
- 長押しでカード詳細が一度だけ開く。
- 短いタップによるカード選択と交換が従来どおり動く。
- カード一覧の縦スクロールを長押しと誤認しない。

## 7. 保存デッキの「プレイヤー解禁」表示削除

### 現状

- 上位大会の解禁はデッキ単位からプレイヤー単位へ変更済み。
- 保存デッキ一覧の各デッキに「プレイヤー解禁 ○○まで」が繰り返し表示されており、デッキ固有情報のように見えるうえ情報量を増やしている。

### 実装方針

1. 保存デッキ一覧の各デッキから「プレイヤー解禁」項目を削除する。
2. デッキ詳細上部の「○○まで・全デッキ共通」表示も、保存デッキ画面の情報として不要なら同時に削除する。
3. 大会エントリー画面では、出場可能な大会を選択肢そのもので示し、解禁状態の説明を重複表示しない。
4. 内部の `tournamentQualification` と出場判定ロジックは削除せず、表示だけを整理する。

### テスト条件

- 保存デッキ一覧とデッキ詳細に「プレイヤー解禁」が表示されない。
- 解禁済み大会への出場可否は変化しない。
- 他のデッキでもプレイヤー単位の上位大会解禁が引き続き有効。

## 8. 特殊合体カード詳細画像の高解像度化

### 現状

- カード図鑑の特殊合体一覧と詳細で、同じ特殊合体でも詳細画像がぼけて見える。
- `special-fusions/` には768×1024の個別画像と、旧アトラスから切り出した横209px程度の低解像度画像が混在している。
- 詳細表示が低解像度の切り出し画像を拡大している特殊合体では、カード一覧よりぼけが目立つ。

### 実装方針

1. 48種すべてについて、実際に参照している画像ファイルと解像度を自動検査する。
2. 横幅が詳細表示のRetina基準を満たさない画像を一覧化し、元の高解像度イラストまたはアトラス原本から再切り出しする。
3. 通常カード表示とカード詳細表示で別の低解像度セルを使い回さず、詳細側には最低でも表示幅の2倍程度の画素数を確保する。
4. 画像軽量化と両立させるため、必要以上に1024pxへ統一せず、詳細表示に十分な640px前後を基準にWebP品質を比較する。
5. 既存のY軸補正、ユーマの横位置補正、カード名帯を避ける位置調整を維持する。

### テスト条件

- 全48種の特殊合体を管理者ツールとカード図鑑詳細で連続確認できる。
- 844×390の詳細画面で輪郭や顔が明確に見え、低解像度画像の引き伸ばしがない。
- イラスト上部の別セル混入、縦方向のずれ、ユーマの顔切れが再発しない。
- 高解像度化後も軽量化目標を大幅に超えない。

## 推奨実装順

1. 奪取画面の欠け修正（局所CSS、危険度が低い）
2. デッキ編集の入替候補ソートと長押し範囲選択防止
3. 保存デッキの不要な「プレイヤー解禁」表示削除
4. リーダーFoilの共通ヘルパーと全表示経路の統一
5. レアリティ正本化と旧資産の無損失移行
6. 特殊合体詳細を含む画像監査スクリプト、遅延読み込み、段階的再圧縮
7. 全テスト、844×390実機相当確認、PWA更新確認、Pages公開

軽量化は変更量が大きいため、正しさに関する3件を先に直し、その後に画像をフォルダー単位で変換する。これにより表示不具合の原因と圧縮差分を混同しない。
