# 今後のゲーム設計候補（2026-08-31）

ユーザー提案を、即時実装・数値検証・仕様確定待ちに分けて管理する。

## 1. マリガン — 数値検証中、未実装

現行スターターは40枚中18枚がモンスター。初手3枚にモンスターが1枚もない確率は15.59%。後攻は第1ターンに手札5枚まで補充されるため、行動開始時にもモンスターがない確率は4.00%。

選んだカードを一時的に山札外へ置き、交換後に戻す一般的な3枚交換を両者が最適に使うと仮定した場合、モンスター不在率は先攻1.94%、後攻0.50%まで下がる。改善幅は先攻の方が大きいため、「両者が同じ3枚交換なら先後勝率が変わらない」とは限らない。

同一デッキ・同一Silver AIの現行ルール20戦では先攻15勝、後攻5勝だった。サンプル数は小さいが、現状は先攻の行動権が強い兆候があるため、対称3枚マリガンをそのまま実装すると先攻有利を強める可能性がある。

推奨する次の比較候補:

1. マリガンなし（現行基準）
2. 後攻のみ最大3枚交換
3. 先攻最大1枚・後攻最大3枚交換
4. 両者最大3枚交換と、別の後攻補正を組み合わせる

実装前にシミュレーターへマリガン方針を追加し、複数デッキ・同一AI・先後入替で比較する。

## 2. 10ターン目覚醒 — 仕様確定待ち、未実装

実装前に以下を決める。

- 先攻10T／後攻10Tの各プレイヤーターン基準か、バトル全体のラウンド10か
- 1試合1回か、各モンスター1回か
- TP、手札、行動権などのコスト
- 能力上昇、技変化、特性解禁、全回復のどれを中心にするか
- 通常モンスター・通常合体・特殊合体で内容を共通化するか
- 覚醒後の速攻化が逆転手段になるか、優勢側の押し切り手段になるか

## 3. 奪取限定カード — 候補抽選の調整案、未実装

Silver以上の同モン類CPUは、モン類ブリーダー5種から2種を採用する。特定の奪取限定1枚が採用される確率は40%。採用が1枚の場合、現行の40枚から5枚を均等抽選する候補出現率は12.5%なので、該当モン類のCPUと戦った1回あたり、欲しい特定1枚が候補に出る概算確率は5%。

奪取限定だけ候補抽選重みを3倍にすると、1枚採用時の候補出現率は12.5%→32.32%、2枚採用時にいずれかが出る確率は23.72%→53.78%。確定入手にはせず、勝利と対戦相手の条件を残したまま徒労感を減らせるため、3倍を第一候補とする。

## 4. トーナメント演出 — v1.21.7で第一段階を実装

- 既存世界観を参照した専用の暗黒幻想コロシアム背景を生成。
- 1599×900、約203KBのWebPへ軽量化。
- 観客席、青緑と金の大会旗、王冠意匠を追加。
- ヘッダー、対戦表、対戦相手欄へ短い入場アニメーションを追加。
- `prefers-reduced-motion`ではアニメーションを停止。

## 5. モンスター名変更 — 対象確認待ち、未実装

既存名を一括変更しない。現行24体を「変更必須候補／一般語として維持候補／要相談」に分類し、イラストから連想できる新名称候補を提示してユーザー確認後に変更する。カード、技、合体レシピ、図鑑、保存データ移行を同時に更新する必要がある。

## 6. バトル演出 — v1.21.7で第一段階を実装

- 攻撃カードの移動に、対象位置の短い着弾リングと火花を追加。
- Trainingと修行の既存バーストへ回転円環と発光を追加。
- 高速モードでも待ち時間を増やさず、低モーション設定ではアニメーションを停止。

